name: Core Contrib Test

on:
  push:
    branches-ignore:
    - 'release/*'
  pull_request:

jobs:
  contrib_0:
    uses: jomcgi/opentelemetry-python-contrib/.github/workflows/core_contrib_test_0.yml@bug-3352/fix-transient-ci-git-errors
    with:
      CORE_REPO_SHA: ${{ github.sha }}
      CONTRIB_REPO_SHA: 01d7ae632fc0e27523f91897e77aced7cde1d09a
